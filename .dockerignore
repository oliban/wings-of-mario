node_modules
shots
tools
tests
docs
.superpowers

# ROM-derived working reference (the disassembly and everything extracted from
# it). Gitignored too. The Dockerfile copies an explicit allow-list so this is
# already excluded today -- this line is here so that a later switch to
# `COPY . .` cannot start shipping it without anyone noticing.
reference

.git
.github
*.md
# package-lock.json is deliberately NOT ignored: the image runs `npm ci`, which
# refuses to install without it. Ignoring it silently turns the build into
# `npm install` territory or fails it outright.
.DS_Store
_j*.js
**/tmpjudge
